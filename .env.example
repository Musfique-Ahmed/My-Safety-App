# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=mysafetydb

# Auth
# IMPORTANT: set a real JWT_SECRET in production (>=32 bytes of randomness).
JWT_SECRET=change-me-please-use-a-32-byte-random-secret
JWT_EXPIRES_MINUTES=120
