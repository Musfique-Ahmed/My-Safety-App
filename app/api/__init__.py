"""App.api: HTTP routers grouped by domain (auth, crimes, chat, ...).

In the current revision the route handlers stay inline in `app.main`;
this package will host the per-domain routers in a follow-up split.
"""
